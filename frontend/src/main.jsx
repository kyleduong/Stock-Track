import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <App />
)

// Add the sorting buttons to the table DONE
// work on the pop up display with graph DONE??

// add a 1 2 3 4 box so you can see more?
// add a timer, so when the program is on it runs every _ seconds or minutes etc.